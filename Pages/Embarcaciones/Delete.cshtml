@page
@model AtlasViewer.Pages.Embarcaciones.DeleteModel
@{
    ViewData["Title"] = "Eliminar Embarcación";
    Layout = "../Shared/_Layout";
}
<section class="container delete-embarcacion-page" aria-labelledby="delete-title">
    <h1 id="delete-title" class="h4 text-danger">Eliminar Embarcación</h1>
    
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="alert alert-warning" role="alert">
                <i class="bi bi-exclamation-triangle-fill"></i>
                <strong>¿Está seguro que desea eliminar esta embarcación?</strong>
                <p class="mb-0 mt-2">Esta acción no se puede deshacer y se actualizará automáticamente el registro del pescador asignado.</p>
            </div>
            
            <div class="card bg-dark text-light">
                <div class="card-body">
                    <dl class="row mb-0">
                        <dt class="col-sm-4">Nombre:</dt>
                        <dd class="col-sm-8">@Model.Embarcacion.nombre</dd>
                        
                        <dt class="col-sm-4">Matrícula:</dt>
                        <dd class="col-sm-8">@Model.Embarcacion.matricula</dd>
                        
                        <dt class="col-sm-4">Propietario:</dt>
                        <dd class="col-sm-8">@(string.IsNullOrEmpty(Model.Embarcacion.propietario) ? "-" : Model.Embarcacion.propietario)</dd>
                        
                        <dt class="col-sm-4">Pescador Asignado:</dt>
                        <dd class="col-sm-8">
                            @if (Model.Pescador != null)
                            {
                                <strong>@Model.Pescador.nombre</strong>
                            }
                            else
                            {
                                <em class="text-muted">Sin asignar</em>
                            }
                        </dd>
                        
                        <dt class="col-sm-4">Estado:</dt>
                        <dd class="col-sm-8">
                            @if (Model.Embarcacion.activo)
                            {
                                <span class="badge bg-success">Activo</span>
                            }
                            else
                            {
                                <span class="badge bg-secondary">Inactivo</span>
                            }
                        </dd>
                    </dl>
                </div>
            </div>
            
            <form method="post" class="mt-3">
                <input type="hidden" asp-for="Embarcacion.Id" />
                <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-danger">Confirmar Eliminación</button>
                    <a asp-page="Index" class="btn btn-secondary">Cancelar</a>
                </div>
            </form>
        </div>
    </div>
</section>
