@page
@model AtlasViewer.Pages.Roles.IndexModel
@{
 ViewData["Title"] = "Roles";
 Layout = "../Shared/_Layout";
}
<section class="container roles-page" aria-labelledby="roles-title">
 <h1 id="roles-title" class="h4">Roles</h1>
 <div class="table-wrap">
 <div class="table-responsive">
 <table class="table align-middle">
 <thead>
 <tr>
 <th>Nombre</th>
 <th>Código</th>
 <th class="text-end">Acciones</th>
 </tr>
 </thead>
 <tbody>
 @foreach (var r in Model.Roles)
 {
 <tr>
 <td>@r.nombre_rol</td>
 <td>@r.id_rol</td>
 <td class="text-end">
 <a asp-page="Edit" asp-route-id="@r.Id" class="btn btn-sm btn-primary">Editar</a>
 <a asp-page="Delete" asp-route-id="@r.Id" class="btn btn-sm btn-outline-danger">Eliminar</a>
 </td>
 </tr>
 }
 </tbody>
 </table>
 </div>
 </div>
 <div class="mt-3">
 <a asp-page="Create" class="btn btn-success">Nuevo Rol</a>
 </div>
</section>